- name: Install MetalLB on existing Minikube cluster
  hosts: target_vm
  become: yes
  vars:
    # MetalLB configuration defaults
    metallb_ip_range_start: "{{ ansible_default_ipv4.address | regex_replace('\\.[0-9]+$', '') }}.200"
    metallb_ip_range_end: "{{ ansible_default_ipv4.address | regex_replace('\\.[0-9]+$', '') }}.254" 
    metallb_ip_range: "{{ metallb_ip_range_start }}-{{ metallb_ip_range_end }}"
    metallb_namespace: "metallb-system"
    metallb_pool_name: "default-pool"
    
    # Allow override via extra vars
    # Example: ansible-playbook install-metallb.yml -e "metallb_ip_range=192.168.1.200-192.168.1.210"
  
  tasks:
    # =============================================================================
    # PRE-FLIGHT CHECKS
    # =============================================================================
    - name: "🔍 PREFLIGHT → Verify minikube is running"
      shell: |
        minikube status --format='{{ "{{" }}.Host{{ "}}" }}'
      become_user: root
      environment:
        HOME: /root
      register: minikube_status
      failed_when: "'Running' not in minikube_status.stdout"
      
    - name: "🔍 PREFLIGHT → Verify kubectl connectivity"
      shell: |
        kubectl get nodes --no-headers
      become_user: root
      environment:
        HOME: /root
      register: kubectl_nodes
      failed_when: kubectl_nodes.rc != 0 or 'Ready' not in kubectl_nodes.stdout
      
    - name: "🔍 PREFLIGHT → Check if MetalLB is already installed"
      shell: |
        kubectl get namespace {{ metallb_namespace }} 2>/dev/null || echo "not-found"
      become_user: root
      environment:
        HOME: /root
      register: metallb_check
      changed_when: false

    # =============================================================================
    # NETWORK CONFIGURATION
    # =============================================================================
    - name: "🌐 NETWORK → Enable IP forwarding for LoadBalancer services"
      sysctl:
        name: net.ipv4.ip_forward
        value: '1'
        state: present
        reload: yes
        
    - name: "🌐 NETWORK → Verify current network configuration"
      debug:
        msg: |
          📡 Network Configuration:
          • Host IP: {{ ansible_default_ipv4.address }}
          • Network: {{ ansible_default_ipv4.network }}/{{ ansible_default_ipv4.netmask }}
          • MetalLB IP Range: {{ metallb_ip_range }}
          • Gateway: {{ ansible_default_ipv4.gateway | default('Not detected') }}

    # =============================================================================
    # METALLB INSTALLATION
    # =============================================================================
    - name: "🚀 METALLB → Enable MetalLB addon in minikube"
      shell: |
        minikube addons enable metallb
      become_user: root
      environment:
        HOME: /root
      register: metallb_addon
      when: "'not-found' in metallb_check.stdout"
      
    - name: "⏳ METALLB → Wait for MetalLB namespace to be created"
      shell: |
        timeout 60 bash -c 'until kubectl get namespace {{ metallb_namespace }} 2>/dev/null; do sleep 2; done'
      become_user: root
      environment:
        HOME: /root
      when: "'not-found' in metallb_check.stdout"
      
    - name: "⏳ METALLB → Wait for MetalLB controller pods to be ready"
      shell: |
        timeout 120 bash -c 'until kubectl get pods -n {{ metallb_namespace }} -o jsonpath="{.items[*].status.phase}" | grep -v Running | wc -l | grep -q "^0$" 2>/dev/null; do sleep 5; done'
      become_user: root
      environment:
        HOME: /root
      when: "'not-found' in metallb_check.stdout"
      retries: 2
      delay: 10

    # =============================================================================
    # METALLB CONFIGURATION
    # =============================================================================
    - name: "📝 METALLB → Check for existing MetalLB configuration"
      shell: |
        kubectl get configmap -n {{ metallb_namespace }} config 2>/dev/null || echo "not-found"
      become_user: root
      environment:
        HOME: /root
      register: existing_pool
      changed_when: false

    - name: "🧹 METALLB → Remove existing broken configuration if present"
      shell: |
        kubectl delete configmap -n {{ metallb_namespace }} config 2>/dev/null || true
      become_user: root
      environment:
        HOME: /root
      when: "'not-found' not in existing_pool.stdout"
      register: config_deleted

    - name: "📝 METALLB → Create MetalLB ConfigMap (legacy v0.9.x format)"
      copy:
        dest: /tmp/metallb-config.yaml
        content: |
          apiVersion: v1
          kind: ConfigMap
          metadata:
            namespace: {{ metallb_namespace }}
            name: config
          data:
            config: |
              address-pools:
              - name: {{ metallb_pool_name }}
                protocol: layer2
                addresses:
                - {{ metallb_ip_range }}
        mode: '0644'
        owner: root
        group: root
      
    - name: "🚀 METALLB → Apply MetalLB configuration"
      shell: |
        kubectl apply -f /tmp/metallb-config.yaml
      become_user: root
      environment:
        HOME: /root
      register: metallb_config_applied
      
    - name: "🧹 METALLB → Clean up configuration file"
      file:
        path: /tmp/metallb-config.yaml
        state: absent

    # =============================================================================
    # AVAHI ADVERTISER INSTALLATION
    # =============================================================================
    - name: "� PYTHON → Install Python 3 and dependencies"
      apt:
        name:
          - python3
          - python3-pip
          - python3-kubernetes
        state: present
        update_cache: yes
      register: python_install
      
    - name: "✅ PYTHON → Verify Python 3 installation"
      shell: |
        python3 --version
        pip3 --version
      register: python_version
      
    - name: "📊 PYTHON → Display Python installation details"
      debug:
        msg: |
          🐍 Python Environment:
          {{ python_version.stdout | indent(2) }}
    
    - name: "�📁 AVAHI → Create temporary directory for avahi-advertiser"
      file:
        path: /tmp/avahi-advertiser
        state: directory
        mode: '0755'
        owner: root
        group: root
      register: avahi_dir_created
      
    - name: "❌ AVAHI ERROR → Failed to create temporary directory"
      debug:
        msg: |
          🚨 ERROR: Failed to create temporary directory /tmp/avahi-advertiser
          
          Details: {{ avahi_dir_created }}
      when: avahi_dir_created.failed | default(false)
      
    - name: "📦 AVAHI → Copy avahi-advertiser directory to target"
      copy:
        src: avahi-advertiser/
        dest: /tmp/avahi-advertiser/
        mode: preserve
        owner: root
        group: root
      register: avahi_copy
      ignore_errors: yes
      
    - name: "❌ AVAHI ERROR → Failed to copy avahi-advertiser files"
      debug:
        msg: |
          🚨 ERROR: Failed to copy avahi-advertiser directory to target node
          
          Possible causes:
          • Source directory 'avahi-advertiser/' not found in playbook directory
          • Permission issues on source or destination
          • Network connectivity problems
          • Insufficient disk space on target
          
          Error details:
          {{ avahi_copy.msg | default('No error message available') }}
          
          ⚠️  Skipping avahi-advertiser installation due to copy failure
      when: avahi_copy.failed | default(false)

    - name: "🚀 AVAHI → Deploy avahi-advertiser service"
      shell: |
        cd /tmp/avahi-advertiser
        bash install.sh
      become_user: root
      register: avahi_install
      ignore_errors: yes
      when: not (avahi_copy.failed | default(false))
      
    - name: "❌ AVAHI ERROR → Failed to install avahi-advertiser"
      debug:
        msg: |
          🚨 ERROR: Failed to install avahi-advertiser service
          
          Possible causes:
          • install.sh script not found or not executable
          • Missing system dependencies (python3, avahi-daemon, etc.)
          • systemd service file issues
          • Permission problems
          • Python package installation failures
          
          Installation output:
          {{ avahi_install.stdout | default('No stdout') | indent(2) }}
          
          Error output:
          {{ avahi_install.stderr | default('No stderr') | indent(2) }}
          
          Return code: {{ avahi_install.rc | default('N/A') }}
          
          ⚠️  You may need to manually troubleshoot the installation
      when: 
        - not (avahi_copy.failed | default(false))
        - avahi_install.failed | default(false)
      
    - name: "✅ AVAHI → Verify avahi-k8s-advertiser service status"
      shell: |
        systemctl status avahi-k8s-advertiser --no-pager
      become_user: root
      register: avahi_status
      failed_when: false
      when: 
        - not (avahi_copy.failed | default(false))
        - not (avahi_install.failed | default(false))
      
    - name: "⚠️  AVAHI WARNING → Service not running properly"
      debug:
        msg: |
          ⚠️  WARNING: avahi-k8s-advertiser service may not be running properly
          
          Service status output:
          {{ avahi_status.stdout | default('Unable to get status') | indent(2) }}
          
          Troubleshooting steps:
          • Check service logs: journalctl -u avahi-k8s-advertiser -n 50
          • Verify kubernetes config: kubectl config view
          • Check avahi daemon: systemctl status avahi-daemon
          • Review service file: /etc/systemd/system/avahi-k8s-advertiser.service
      when: 
        - not (avahi_copy.failed | default(false))
        - not (avahi_install.failed | default(false))
        - avahi_status.rc != 0
      
    - name: "📊 AVAHI → Display avahi-advertiser installation status"
      debug:
        msg: |
          ✅ Avahi Advertiser Installation Successful
          
          📡 Installation Output:
          {{ avahi_install.stdout | indent(2) }}
          
          🔍 Service Status:
          {{ avahi_status.stdout | indent(2) }}
      when: 
        - not (avahi_copy.failed | default(false))
        - not (avahi_install.failed | default(false))
        - avahi_status.rc == 0

    - name: "🧹 AVAHI → Clean up temporary directory"
      file:
        path: /tmp/avahi-advertiser
        state: absent
      when: avahi_dir_created.changed | default(false)

    # =============================================================================
    # VERIFICATION AND TESTING
    # =============================================================================
    - name: "✅ VERIFY → Check MetalLB pods status"
      shell: |
        kubectl get pods -n {{ metallb_namespace }} -o wide
      become_user: root
      environment:
        HOME: /root
      register: metallb_pods
      
    - name: "✅ VERIFY → Check MetalLB configuration"
      shell: |
        kubectl get configmap -n {{ metallb_namespace }} config -o yaml
      become_user: root
      environment:
        HOME: /root
      register: metallb_resources
      
    - name: "📊 VERIFY → Display MetalLB status"
      debug:
        msg: |
          🎯 MetalLB Installation Status:
          {{ metallb_pods.stdout | indent(2) }}
          
          📡 MetalLB Configuration:
          {{ metallb_resources.stdout | indent(2) }}

    # =============================================================================
    # TEST SERVICE DEPLOYMENT (OPTIONAL)
    # =============================================================================
    - name: "🧪 TEST → Create test nginx deployment and LoadBalancer service"
      copy:
        dest: /tmp/metallb-test.yaml
        content: |
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: metallb-test-nginx
            labels:
              app: metallb-test-nginx
          spec:
            replicas: 1
            selector:
              matchLabels:
                app: metallb-test-nginx
            template:
              metadata:
                labels:
                  app: metallb-test-nginx
              spec:
                containers:
                - name: nginx
                  image: nginx:alpine
                  ports:
                  - containerPort: 80
                  resources:
                    limits:
                      memory: "128Mi"
                      cpu: "100m"
          ---
          apiVersion: v1
          kind: Service
          metadata:
            name: metallb-test-nginx
          spec:
            selector:
              app: metallb-test-nginx
            ports:
            - protocol: TCP
              port: 80
              targetPort: 80
            type: LoadBalancer
        mode: '0644'
      when: test_service is defined and test_service | bool
      
    - name: "🧪 TEST → Deploy test nginx service"
      shell: |
        kubectl apply -f /tmp/metallb-test.yaml
      become_user: root
      environment:
        HOME: /root
      when: test_service is defined and test_service | bool
      
    - name: "⏳ TEST → Wait for LoadBalancer IP assignment"
      shell: |
        timeout 60 bash -c 'until kubectl get svc metallb-test-nginx -o jsonpath="{.status.loadBalancer.ingress[0].ip}" 2>/dev/null | grep -E "^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$"; do sleep 2; done'
      become_user: root
      environment:
        HOME: /root
      register: lb_ip
      when: test_service is defined and test_service | bool
      
    - name: "🎯 TEST → Display LoadBalancer service info"
      shell: |
        kubectl get svc metallb-test-nginx -o wide
      become_user: root
      environment:
        HOME: /root
      register: test_svc_info
      when: test_service is defined and test_service | bool
      
    - name: "📊 TEST → Show test service details"
      debug:
        msg: |
          🧪 Test LoadBalancer Service:
          {{ test_svc_info.stdout | indent(2) }}
          
          🌐 Test the service:
          curl http://{{ lb_ip.stdout }}
          
          🧹 Clean up test service:
          kubectl delete -f /tmp/metallb-test.yaml
      when: test_service is defined and test_service | bool
      
    - name: "🧹 TEST → Clean up test configuration file"
      file:
        path: /tmp/metallb-test.yaml
        state: absent
      when: test_service is defined and test_service | bool

    # =============================================================================
    # COMPLETION STATUS
    # =============================================================================
    - name: "🏁 COMPLETION → Display installation summary"
      debug:
        msg: |
          ✅ MetalLB installation completed successfully!
          
          📋 Configuration Summary:
          • MetalLB Namespace: {{ metallb_namespace }}
          • IP Address Pool: {{ metallb_ip_range }}
          • Pool Name: {{ metallb_pool_name }}
          • Protocol: Layer 2 (ARP-based)
          • Host Network: {{ ansible_default_ipv4.address }}/{{ ansible_default_ipv4.netmask }}
          
          🚀 Usage Instructions:
          1. Create a deployment:
             kubectl create deployment my-app --image=nginx
          
          2. Expose as LoadBalancer:
             kubectl expose deployment my-app --type=LoadBalancer --port=80
          
          3. Check assigned IP:
             kubectl get svc my-app
          
          🔧 Management Commands:
          • List LoadBalancer services: kubectl get svc --field-selector spec.type=LoadBalancer
          • Check MetalLB logs: kubectl logs -n {{ metallb_namespace }} -l app=metallb
          • View IP pools: kubectl get ipaddresspool -n {{ metallb_namespace }}
          • Monitor L2 advertisements: kubectl get l2advertisement -n {{ metallb_namespace }}
          
          📡 Network Notes:
          • LoadBalancer IPs are directly accessible from the host network
          • IPs are assigned from range: {{ metallb_ip_range }}
          • Ensure this range doesn't conflict with DHCP
          • Services are reachable without port forwarding
          
          🧪 Quick Test:
          ansible-playbook install-metallb.yml -e "test_service=true"
          
          ⚠️  Important:
          • MetalLB uses Layer 2 mode (ARP) for IP advertisement
          • Assigned IPs must be on the same subnet as the host
          • Configure firewall rules if needed for external access